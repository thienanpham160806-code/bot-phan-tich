# --- lõi ---
python-dotenv>=1.0
pyyaml>=6.0
pandas>=2.2
numpy>=1.26
pyarrow>=16.0
tenacity>=8.2

# --- nguồn dữ liệu ---
# `pip install openapi-sdk` (SDK chính thức của DNSE) KHÔNG cài được — package
# chưa publish thật lên PyPI (đã xác nhận 21/09/2026), dù docs của DNSE nói vậy.
# Repo Git cũng không có setup.py/pyproject.toml. Giải pháp tạm: vendor cục bộ
# tại vendor/dnse-sdk/ — cài bằng `pip install -e vendor/dnse-sdk` (không nằm
# trong file requirements.txt vì đây là đường dẫn cục bộ, không phải PyPI).
# Các dòng dưới là dependency THẬT của SDK đó (xem vendor/dnse-sdk/pyproject.toml):
urllib3>=2.0
certifi
websockets>=12.0
msgpack>=1.0
vnstock>=4.0              # Vietcap/VCI: báo cáo tài chính, danh sách mã, ngành (nguồn dự phòng)
httpx>=0.27
requests>=2.31             # data/vietcap.py: nạp toàn sàn theo lô (endpoint công khai bảng giá)

# --- backtest ---
scipy>=1.11                # backtest/metrics.py: deflated_sharpe (scipy.stats.norm)

# --- text mining báo cáo tài chính ---
pdfplumber>=0.11            # analysis/fintext.py: extract_text()

# --- bot & lịch ---
aiogram>=3.7
apscheduler>=3.10

# --- biểu đồ ---
matplotlib>=3.8

# --- kiểm thử & chất lượng mã ---
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4
