# --- lõi ---
python-dotenv>=1.0
pyyaml>=6.0
pandas>=2.2
numpy>=1.26
pyarrow>=16.0
tenacity>=8.2

# --- nguồn dữ liệu ---
# SDK chính thức của DNSE: tên gói PyPI thật là "dnse-sdk-openapi" (đã xác nhận
# trên PyPI 22/09/2026) — KHÔNG phải "openapi-sdk" như docs của DNSE ghi nhầm.
# Import trong code vẫn là `from dnse import DNSEClient` (xem data/dnse.py).
dnse-sdk-openapi>=2.3
# Gói trên KHÔNG tự khai báo dependency (METADATA rỗng), nhưng dnse/__init__.py
# import TradingClient (module websocket) ngay khi import cả gói — nên vẫn cần
# cài thủ công 4 dòng dưới (dnse/websocket/*.py cần chúng), dù bot chỉ dùng
# DNSEClient (REST), không dùng TradingClient (WebSocket):
urllib3>=2.0
certifi
websockets>=12.0
msgpack>=1.0
vnstock>=4.0              # Vietcap/VCI: báo cáo tài chính, danh sách mã, ngành (nguồn dự phòng)
httpx>=0.27
requests>=2.31             # data/vietcap.py: nạp toàn sàn theo lô (endpoint công khai bảng giá)

# --- backtest ---
scipy>=1.11                # backtest/metrics.py: deflated_sharpe (scipy.stats.norm)

# --- text mining báo cáo tài chính ---
pdfplumber>=0.11            # analysis/fintext.py: extract_text()

# --- bot & lịch ---
aiogram>=3.7
apscheduler>=3.10

# --- biểu đồ ---
matplotlib>=3.8

# --- kiểm thử & chất lượng mã ---
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4
