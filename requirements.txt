# --- lõi ---
python-dotenv>=1.0
pyyaml>=6.0
pandas>=2.2
numpy>=1.26
pyarrow>=16.0
tenacity>=8.2

# --- nguồn dữ liệu ---
openapi-sdk>=0.1          # SDK chính thức của DNSE
vnstock>=4.0              # Vietcap/VCI: báo cáo tài chính, danh sách mã, ngành
httpx>=0.27

# --- backtest ---
scipy>=1.11                # backtest/metrics.py: deflated_sharpe (scipy.stats.norm)

# --- text mining báo cáo tài chính ---
pdfplumber>=0.11            # analysis/fintext.py: extract_text()

# --- bot & lịch ---
aiogram>=3.7
apscheduler>=3.10

# --- biểu đồ ---
matplotlib>=3.8

# --- kiểm thử & chất lượng mã ---
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4
